<%= render :partial => 'view_customizes/header' %>

<h2><%= link_to l(:label_view_customize_plural), view_customizes_path %> &#187; <%=l(:label_view_customizes_new) %></h2>

<%= labelled_form_for :view_customize, @view_customize,
        :url => view_customizes_path,
        :html => {:multipart => true, :id => 'view_customize-form'} do |f| %>
    <%= render :partial => 'view_customizes/form', :locals => {:form => f} %>
    <%= f.submit l(:button_create) %>
    <%= link_to l(:button_cancel), :controller => 'view_customizes', :action => 'index' %>
<% end %>
